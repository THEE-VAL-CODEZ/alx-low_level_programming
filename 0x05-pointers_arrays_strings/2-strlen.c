#include "main.h"

/**
 *  _strlen - returns the legnth of a string
 *
 *  @s: char type pointer
 *
 *  Return: legnth
 */

int _strlen(char *s)
{
	int c;

	for (c = 0; s[c] != '\0'; c++)
	{
		return (c);
	}
}
